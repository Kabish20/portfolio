services:
  - type: web
    name: portfolio
    runtime: node
    nodeVersion: 18
    buildCommand: cd ultra-portfolio && npm install && npm run build
    startCommand: npm install -g serve && serve -s ultra-portfolio/dist -l $PORT
    envVars:
      - key: NODE_ENV
        value: production
