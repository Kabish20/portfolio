services:
  - type: web
    name: portfolio
    runtime: node
    nodeVersion: 18
    buildCommand: cd ultra-portfolio && npm ci && npm run build
    startCommand: cd ultra-portfolio && npx serve -s dist -l $PORT
    envVars:
      - key: NODE_ENV
        value: production
